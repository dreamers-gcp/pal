# If Xcode reports "node: command not found" during build, copy this file to:
#   mobile/ios/.xcode.env.local
# and set NODE_BINARY to the output of `which node` on your Mac.
#
# export NODE_BINARY=/opt/homebrew/bin/node
# export NODE_BINARY=/usr/local/bin/node
