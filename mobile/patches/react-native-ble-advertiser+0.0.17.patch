diff --git a/node_modules/react-native-ble-advertiser/android/src/main/java/com/vitorpamplona/bleadvertiser/BLEAdvertiserModule.java b/node_modules/react-native-ble-advertiser/android/src/main/java/com/vitorpamplona/bleadvertiser/BLEAdvertiserModule.java
index 036de18..f8d81b9 100644
--- a/node_modules/react-native-ble-advertiser/android/src/main/java/com/vitorpamplona/bleadvertiser/BLEAdvertiserModule.java
+++ b/node_modules/react-native-ble-advertiser/android/src/main/java/com/vitorpamplona/bleadvertiser/BLEAdvertiserModule.java
@@ -467,7 +467,7 @@ public class BLEAdvertiserModule extends ReactContextBaseJavaModule {
             dataBuilder.setIncludeTxPowerLevel(options.getBoolean("includeTxPowerLevel"));
         
         dataBuilder.addManufacturerData(companyId, payload);
-        dataBuilder.addServiceUuid(uuid);
+        // Service UUID omitted — 128-bit UUID + manufacturer data exceeds legacy 31-byte AD limit.
         return dataBuilder.build();
     }
 
diff --git a/node_modules/react-native-ble-advertiser/ios/BLEAdvertiser.h b/node_modules/react-native-ble-advertiser/ios/BLEAdvertiser.h
index 61624a3..1494cd0 100644
--- a/node_modules/react-native-ble-advertiser/ios/BLEAdvertiser.h
+++ b/node_modules/react-native-ble-advertiser/ios/BLEAdvertiser.h
@@ -6,6 +6,7 @@
 @interface BLEAdvertiser : RCTEventEmitter <RCTBridgeModule, CBCentralManagerDelegate, CBPeripheralManagerDelegate, CBPeripheralDelegate> {
     CBCentralManager *centralManager;
     CBPeripheralManager *peripheralManager;
+    int meshCompanyId;
 }
 
 @end
diff --git a/node_modules/react-native-ble-advertiser/ios/BLEAdvertiser.m b/node_modules/react-native-ble-advertiser/ios/BLEAdvertiser.m
index 38693ab..6e48492 100644
--- a/node_modules/react-native-ble-advertiser/ios/BLEAdvertiser.m
+++ b/node_modules/react-native-ble-advertiser/ios/BLEAdvertiser.m
@@ -18,6 +18,7 @@ - (dispatch_queue_t)methodQueue
 
 RCT_EXPORT_METHOD(setCompanyId: (nonnull NSNumber *)companyId){
     RCTLogInfo(@"setCompanyId function called %@", companyId);
+    meshCompanyId = [companyId intValue];
     self->centralManager = [[CBCentralManager alloc] initWithDelegate:self queue: nil options:@{CBCentralManagerOptionShowPowerAlertKey: @(YES)}];
     self->peripheralManager = [[CBPeripheralManager alloc] initWithDelegate:self queue:nil options:nil];
 }
@@ -27,13 +28,21 @@ - (dispatch_queue_t)methodQueue
     rejecter:(RCTPromiseRejectBlock)reject){
 
     RCTLogInfo(@"Broadcast function called %@ at %@", uid, payload);
-    // Beacon Version. 
-    //NSUUID *proximityUUID = [[NSUUID alloc] initWithUUIDString:uid];
-    //CLBeaconRegion *beaconRegion = [[CLBeaconRegion alloc] initWithProximityUUID:proximityUUID major:1 minor:1 identifier:REGION_ID];
-    //NSDictionary *advertisingData = [beaconRegion peripheralDataWithMeasuredPower:nil];
-    
+    // Manufacturer data only (upstream iOS only advertised service UUIDs and ignored payload).
+    NSMutableData *mfg = [NSMutableData data];
+    uint16_t cid = (uint16_t)meshCompanyId;
+    uint8_t lo = cid & 0xff;
+    uint8_t hi = (cid >> 8) & 0xff;
+    [mfg appendBytes:&lo length:1];
+    [mfg appendBytes:&hi length:1];
+    for (NSNumber *n in payload) {
+        int v = [n intValue] & 0xff;
+        uint8_t b = (uint8_t)v;
+        [mfg appendBytes:&b length:1];
+    }
     NSDictionary *advertisingData = @{
-        CBAdvertisementDataServiceUUIDsKey : @[[CBUUID UUIDWithString:uid]]};
+        CBAdvertisementDataManufacturerDataKey : mfg,
+    };
 
     [peripheralManager startAdvertising:advertisingData];
 
